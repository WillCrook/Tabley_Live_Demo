import BookingCalendar from "@/components/BookingCalendar";

const Bookings = () => {
  return (
    <div className="p-6 pt-2 h-[calc(100vh-100px)]">
      <BookingCalendar className="h-full" />
    </div>
  );
};

export default Bookings;
